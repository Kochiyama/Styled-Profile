import React from 'react';

const Profile = ({
  avatar,
  backgroundImg,
  fullname,
  username,
  email,
  address,
  bio,
  instagramLink,
  linkedinLink,
}) => {
  return <h1>Profile Page</h1>;
};

export default Profile;
